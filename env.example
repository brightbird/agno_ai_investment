# Agno AI 投资分析系统环境变量配置

# 阿里云通义千问 API 密钥
# 获取地址: https://bailian.console.aliyun.com/
ALIYUN_API_KEY=your_aliyun_api_key_here

# 可选：指定模型ID（默认使用 qwen-plus-latest）
# 支持的模型: qwen-turbo, qwen-plus, qwen-max, qwen-plus-latest, qwen-max-latest
DEFAULT_MODEL_ID=qwen-plus-latest

# 可选：数据库存储路径（默认使用项目内的 data/agent_storage/）
# DATABASE_PATH=./data/agent_storage/investment_agents.db

# 可选：日志级别（默认: INFO）
# LOG_LEVEL=INFO

# 可选：服务器配置
# HOST=localhost
# PORT=7777

# 开发模式（默认: False）
# DEV_MODE=False

# 注意：
# 1. 复制此文件为 .env：cp env.example .env
# 2. 替换上述占位符为真实的API密钥
# 3. .env 文件已在 .gitignore 中排除，不会被提交到版本控制系统
# 4. 获取阿里云API密钥后，请保管好您的密钥，不要泄露给他人 